@page "{id:int}"
@model MuntiRomania.Pages.MountainRanges.DetailsModel

<h1>Mountain Range Details</h1>

<div>
    <dl class="row">
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.MountainRange.Name</dd>

        <dt class="col-sm-2">Region</dt>
        <dd class="col-sm-10">@Model.MountainRange.Region?.Name</dd>
    </dl>
</div>

<p>
    <a asp-page="Edit" asp-route-id="@Model.MountainRange.MountainRangeId">Edit</a> |
    <a asp-page="Index">Back to List</a>
</p>
