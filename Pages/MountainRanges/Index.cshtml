@page
@model MuntiRomania.Pages.MountainRanges.IndexModel

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-body">

            <div class="d-flex justify-content-between align-items-center mb-3">
                <h1 class="h3 mb-0">Mountain Ranges</h1>
                <a asp-page="Create" class="btn btn-success">+ Add Mountain Range</a>
            </div>

            <table class="table table-hover align-middle">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Region</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.MountainRange)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.Region?.Name</td>
                            <td class="text-end">
                                <a asp-page="Edit" asp-route-id="@item.MountainRangeId" class="btn btn-sm btn-outline-primary">Edit</a>
                                <a asp-page="Details" asp-route-id="@item.MountainRangeId" class="btn btn-sm btn-outline-secondary">Details</a>
                                <a asp-page="Delete" asp-route-id="@item.MountainRangeId" class="btn btn-sm btn-outline-danger">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>
</div>
