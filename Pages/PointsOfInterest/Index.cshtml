@page
@model MuntiRomania.Pages.PointsOfInterest.IndexModel

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-body">

            <div class="d-flex justify-content-between align-items-center mb-3">
                <h1 class="h3 mb-0">Points of Interest</h1>
                <a asp-page="Create" class="btn btn-success">+ Add POI</a>
            </div>

            <table class="table table-hover align-middle">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Type</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Points)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.Type</td>
                            <td class="text-end">
                                <a asp-page="Edit" asp-route-id="@item.PointOfInterestId" class="btn btn-sm btn-outline-primary">Edit</a>
                                <a asp-page="Details" asp-route-id="@item.PointOfInterestId" class="btn btn-sm btn-outline-secondary">Details</a>
                                <a asp-page="Delete" asp-route-id="@item.PointOfInterestId" class="btn btn-sm btn-outline-danger">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>
</div>
